import {test, expect, request} from '@playwright/test';



export default class BasePage{
// const candidateBtn = test('API cordinate with UI', async({page, request}) => {
//    const response = await request.get('/recruitment/candidates?limit=50&offset=0&model=list&sortField=candidate.dateOfApplication&sortOrder=DESC');
//    expect(response.ok()).toBe(true);
   
   
// });
}